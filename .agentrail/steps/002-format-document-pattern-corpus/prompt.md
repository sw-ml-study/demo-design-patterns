Run the canonical ../sw-mlpl/scripts/mlpl-fmt.sh across every tracked .mlpl file, audit every user function for a doc string and each demo for representation/invariant/complexity/copy/loop comments, and add routine format/doc checks to just check. Keep web/*.mlpl standalone with no include. Use failing-then-passing checks, run mlplunit full gate, commit with details, push, then complete.